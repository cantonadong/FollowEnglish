系统环境
已安装 python，npm,node,android cli, go,rustc,github cli
若需要安装新工具，尽量安装到 D:\Program\ 下，安装后，记得自动添加脚本到全局变量，方便后续调用

0. 执行代码首选 git bash 执行，不兼容的再考虑 powershell 或 cmd
0. 不要尝试装 winget
1. 理解需求，编写需求开发文档。需求内容包括业务流程、页面\交互\功能逻辑，视觉交互效果。开发内容包括：使用架构（前端、后端、交互、视觉、数据等），开发顺序，相互依赖等。确保100%还原用户已给出文档所有功能、逻辑、视觉、交互细节
2. 记录log每个任务节点完成状态，记录可提升后续开发迭代有用的已修复问题信息（如开发环境、工具引用、bug、视觉/交互设计、功能逻辑等），待处理的任务和说明。有用的开发环境信息等
2. 完成每个模块后，更新log并交付给用户测试验证（生成apk，或给可直接运行的绿色版bat，exe文件等）
3. 用户测试通过后，更新log并继续下一个任务
4.申请任何工具联网权限时，如果第一次无法联网，请新开进程并10秒后重试一次。因为用户安装了防火墙，确认联网需要时间，且同进程可能即使点击了通过联网也无法直接联网
5. 如果有适用于全局开发的背景信息、规范、资料，经验、踩过的坑等，可更新到本文档内
6. 生成文件时，永远不要包含个人隐私信息，或能猜到的可能人名、号码等信息


## UI / Animation Preview First Rule

凡涉及 UI、布局、样式、动效、交互反馈的修改，必须先做可预览版本，再写入真实业务逻辑。

要求：

1. 先创建或更新独立 Preview / Playground / Demo 页面，不依赖真实接口、登录、数据库、权限或完整打包流程。
2. UI 必须使用 mock data 还原最终效果，至少覆盖 normal、loading、empty、error、edge case 状态。
3. 动效必须能在 Preview 中直接触发、暂停、重复查看，不能只
7. 优先复用已有组件、样式、mock data 和 preview 入口，避免重复创建，减少 token 和代码改动。
8. 如必须修改业务逻辑，必须先说明原因、影响范围和回滚方式，再执行。
能通过完整业务流程查看。
4. 预览效果必须尽量 1:1 对齐最终页面，包括尺寸、颜色、间距、字体、状态变化、动效节奏和交互反馈。
5. 在用户确认 UI / 动效效果前，不得写入或大改真实业务逻辑代码。
6. 每次完成后只输出最少必要信息：

* 修改了什么
* 预览入口在哪里
* 如何运行
* 是否改动业务逻辑
* 还需要用户确认什么


同步到github时，永远不要上传需求文档和开发规范
